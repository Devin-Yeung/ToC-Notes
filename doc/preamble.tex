\usepackage[table]{xcolor}
\usepackage{adjustbox}
\usepackage{setspace} % Related Command: \setstretch
\setstretch{1.5}

% Better enumeration
\usepackage{enumerate}

% Algorithm support
\usepackage[linesnumbered,ruled,vlined]{algorithm2e}
\SetKwInput{KwInput}{Input} % Set the Input
\SetKwInput{KwOutput}{Output} % set the Output

% Indention
\usepackage{indentfirst}

% Better subcaption
\usepackage{subcaption}

% More math symbols
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{amsthm}
\newtheorem{definition}{Definition}
\usepackage{mathtools}
\allowdisplaybreaks % page breaks in equation is allowed

% Support Tables
\usepackage{booktabs} % Related Command: \toprule \midrule \bottomrule
\usepackage{hyperref}
\usepackage{fancyvrb}

% Better \ref command
\usepackage{cleveref}
\crefname{lstlisting}{listing}{listings}
\Crefname{lstlisting}{Listing}{Listings}

\usepackage{listings}
% usage example:
% define some basic colors
\definecolor{mauve}{rgb}{0.58,0,0.82}
\lstdefinestyle{ANSI-C} {
	basicstyle=\ttfamily,                        % very small code
	breaklines=true,                             % break long lines
	commentstyle=\itshape\color{green!50!black}, % comments are green
	keywordstyle=[1]\color{blue!80!black},       % instructions are blue
	keywordstyle=[2]\color{orange!80!black},     % sections/other directives are orange
	keywordstyle=[3]\color{red!50!black},        % registers are red
	stringstyle=\color{mauve},                   % strings are from the telekom
	% identifierstyle=\color{teal},              % user declared addresses are teal
	frame=shadowbox,                             % black line on the left side of code
	linewidth=\textwidth,                        % centering the box
	xleftmargin=0.1\textwidth,                   % centering the box
	xrightmargin=0.1\textwidth,                  % centering the box
	language=[ansi]C,                            % all code is ansi-c
	tabsize=4,                                   % indent tabs with 4 spaces
	showstringspaces=false,                      % do not replace spaces with weird underlines
	keepspaces=true,                             % keep the comment alignment
	columns=fullflexible
}


% Pre-defined env
\lstnewenvironment{ANSI-C}
{\lstset{style=ANSI-C}}
{}
\newcommand{\ic}[1]{\colorbox[rgb]{0.95, 0.95, 0.95}{\lstinline[style=ANSI-C]$#1$}}
% Set Margins
\setlength{\topmargin}{-0.5in}
\setlength{\textheight}{9in}
\setlength{\oddsidemargin}{0in}
\setlength{\evensidemargin}{0in}
\setlength{\textwidth}{6.5in}

% Better table
\usepackage{array}

% User-Defined Command
% \TODO command
\newcommand\TODO{{\bfseries \color{red} \itshape \underline{Todo}}}


% Credit to: https://github.com/SeniorMars/dotfiles/blob/master/latex_template
\usepackage{tikz}
\usepackage[most,many,breakable]{tcolorbox}

%================================
% DEFINITION
%================================
\definecolor{defbg}{HTML}{f2fbfc}
\definecolor{deffr}{HTML}{191971}
\tcbuselibrary{theorems,skins,hooks}
\newtcbtheorem[number within=section]{Definition}{Definition}
{%
	enhanced,
	breakable,
	colback = defbg,
	frame hidden,
	boxrule = 0sp,
	borderline west = {2pt}{0pt}{deffr},
	sharp corners,
	detach title,
	before upper = \tcbtitle\par\smallskip,
	coltitle = deffr,
	fonttitle = \bfseries\sffamily,
	description font = \mdseries,
	separator sign none,
	segmentation style={solid, deffr},
}
{th}

%================================
% THEOREM
%================================
\definecolor{theorembg}{HTML}{F2F2F9}
\definecolor{theoremfr}{HTML}{00007B}

\tcbuselibrary{theorems,skins,hooks}
\newtcbtheorem[number within=section]{Theorem}{Theorem}
{%
	enhanced,
	breakable,
	colback = theorembg,
	frame hidden,
	boxrule = 0sp,
	borderline west = {2pt}{0pt}{theoremfr},
	sharp corners,
	detach title,
	before upper = \tcbtitle\par\smallskip,
	coltitle = theoremfr,
	fonttitle = \bfseries\sffamily,
	description font = \mdseries,
	separator sign none,
	segmentation style={solid, theoremfr},
}
{th}

\definecolor{examplebg}{HTML}{F2FBF8}
\definecolor{examplefr}{HTML}{88D6D1}
\definecolor{exampleti}{HTML}{2A7F7F}

%================================
% EXAMPLE BOX
%================================

\newtcbtheorem[number within=section]{Example}{Example}
{%
	colback = examplebg
	,breakable
	,colframe = examplefr
	,coltitle = exampleti
	,boxrule = 1pt
	,sharp corners
	,detach title
	,before upper=\tcbtitle\par\smallskip
	,fonttitle = \bfseries
	,description font = \mdseries
	,separator sign none
	,description delimiters parenthesis
}
{ex}

\definecolor{lemmabg}{HTML}{FFFAF8}
\definecolor{lemmafr}{HTML}{983b0f}


%================================
% LEMMA
%================================

\tcbuselibrary{theorems,skins,hooks}
\newtcbtheorem[number within=section]{Lemma}{Lemma}
{%
	enhanced,
	breakable,
	colback = lemmabg,
	frame hidden,
	boxrule = 0sp,
	borderline west = {2pt}{0pt}{lemmafr},
	sharp corners,
	detach title,
	before upper = \tcbtitle\par\smallskip,
	coltitle = lemmafr,
	fonttitle = \bfseries\sffamily,
	description font = \mdseries,
	separator sign none,
	segmentation style={solid, lemmafr},
}
{th}
